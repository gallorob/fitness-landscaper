from setuptools import setup

setup(
    name='fitness-landscaper',
    version='0.0.1',
    packages=['fitland'],
    install_requires=[
        'matplotlib',
        'numpy',
        'scipy'
      ],
  )